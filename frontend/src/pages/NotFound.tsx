import { Box } from "@mui/material";

const NotFound = () => {
    return <Box sx={{fontWeight: 700, fontSize: '40px', textAlign: 'center', marginTop: '40px'}}>NO PAGE FOUND</Box>;
};

export default NotFound;
